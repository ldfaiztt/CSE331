# Tests if a node can be added to the graph

# Create a graph
created graph one_node_graph

# Add a node
added node a to one_node_graph

# Print the nodes in the graph and the outgoing edges from a
one_node_graph contains: a
the children of a in one_node_graph are: